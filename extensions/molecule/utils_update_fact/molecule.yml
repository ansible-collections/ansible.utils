---
# Migrated from tests/integration/targets/utils_update_fact
# Shared ansible-native config + inventory live in extensions/molecule/
# (../config.yml, ../inventory.yml). Override here only when needed.
