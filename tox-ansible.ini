[tox]
ignore_base_python_conflict = true

[ansible]

# Interim: run Molecule for integration until tox-ansible has a first-class
# molecule runner. Keep brace lists aligned with `tox -l --ansible`.
# commands_pre (ade install) is unchanged.
# Used by ansible-content-actions via: tox --ansible --conf tox-ansible.ini
[testenv:integration-py{3.11,3.12,3.13,3.14}-{2.16,2.18,2.19,2.20,2.21,devel,milestone}]
commands =
    python -m molecule test --all --workers 4
